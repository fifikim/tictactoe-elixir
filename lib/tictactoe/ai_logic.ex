defmodule TicTacToe.AiLogic do
  alias TicTacToe.Board
  alias TicTacToe.ConsoleIO
  alias TicTacToe.Game
  alias TicTacToe.WinFinder

  def find_best(cells, markers) do
    cells
    # |> if available
    # |> update board with marker
    # |> get score (call minimax)
  end
end
